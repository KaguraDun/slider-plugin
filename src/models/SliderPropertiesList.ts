enum sliderPropertiesList {
  from = 'setFrom',
  min = 'setMin',
  max = 'setMax',
  step = 'setStep',
  to = 'setTo',
  showBar = 'setShowBar',
  showScale = 'setShowScale',
  showTip = 'setShowTip',
  isRange = 'setIsRange',
  isVertical = 'setIsVertical',
}

export default sliderPropertiesList;
